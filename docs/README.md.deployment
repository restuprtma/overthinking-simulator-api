
## 📦 Deployment

Proyek ini siap di-deploy ke Kubernetes menggunakan CI/CD dengan Jenkins.

### Struktur Deployment

```
├── .ci/                    # CI/CD configuration (Jenkins)
├── deployments/
│   ├── docker/            # Docker entrypoint script
│   └── kubernetes/        # Kubernetes manifests untuk production
├── Dockerfile             # Multi-stage Docker build
└── DEPLOYMENT.md          # 📖 Panduan deployment lengkap
```

### Quick Start

**Build Docker Image:**
```bash
docker build -t your-registry/lakukan-api:latest .
docker push your-registry/lakukan-api:latest
```

**Deploy to Kubernetes:**
```bash
kubectl apply -f deployments/kubernetes/k8s-deployment.yaml
```

**Dokumentasi Lengkap:** Lihat [DEPLOYMENT.md](DEPLOYMENT.md) dan [README.Docker.md](README.Docker.md)

